apiVersion: v1
kind: Service
metadata:
  name: jenkins-cicd-demo-svc
spec:
  type: NodePort
  selector:
    app: jenkins-cicd-demo
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
